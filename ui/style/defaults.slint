export global MainWindow {
    out property <brush> background: #ffffff;

    out property <length> width: 1024px;
    out property <length> height: 768px;

    out property <length> font-size: 16px;
    
}