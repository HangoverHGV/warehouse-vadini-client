import { Button, VerticalBox } from "std-widgets.slint";
import { MainWindowStyle } from "style/defaults.slint";
import { NavBar } from "components/nav.slint";
import { Filter } from "components/filter.slint";
import { ProductContainer } from "components/product-container.slint";

export component Main inherits Window {
    width: MainWindowStyle.width;
    height: MainWindowStyle.height;
    background: MainWindowStyle.background;

    VerticalLayout {
        spacing: 5px;
        alignment: start;
        NavBar {
        }
        Filter {
            x: 10px;
        }
        HorizontalLayout {
            x: 10px;
            ProductContainer {
                title: "Geka Elit 6013";
                image: @image-url("mockup-image/GekaElit6013.png");
                description: "Electrozi 6013";
                category: "Electrozi";
                price: "Pret: 50 RON";
            }
        }   

    }
}