
export global MainWindowStyle {
    out property <brush> background: #f0f2f5;
}

export global NavbarStyle {
    out property <brush> background: #1a2f4a;

    out property <length> height: 60px;
    out property <length> admin-height: 112px;

    out property <length> font-size: 15px;
}

export global ButtonStyles {
    out property <brush> default-background: #3b82f6;
    out property <brush> success-background: #22c55e;
    out property <brush> danger-background: #ef4444;

    out property <brush> default-foreground: #ffffff;
    out property <brush> success-foreground: #ffffff;
    out property <brush> danger-foreground: #ffffff;

    out property <int> button-text-weight: 500;
    out property <length> button-text-size: 14px;

    out property <length> button-width: 110px;
    out property <length> button-height: 38px;

    out property <length> button-border-radius: 8px;
}

export global OneLineInputStyles {
    out property <brush> font-color: #1e293b;
    out property <brush> garied-font-color: #94a3b8;
    out property <length> font-size: 14px;

    out property <length> width: 200px;
    out property <length> height: 38px;

    out property <brush> background: #ffffff;
    out property <brush> border-color: #cbd5e1;
    out property <length> border-width: 1px;
    out property <length> border-radius: 8px;
}

export global DropdownStyles {
    out property <brush> font-color: #1e293b;
    out property <brush> garied-background-color: #f8fafc;
    out property <length> font-size: 14px;
    out property <int> font-weight: 400;
    out property <brush> border-color: #cbd5e1;
}

export global FilterStyles {
    out property <brush> background: #ffffff;
    out property <brush> border-color: #e2e8f0;
    out property <length> border-width: 1px;
    out property <length> border-radius: 8px;
    out property <length> width: 300px;
    out property <length> height: 44px;

    out property <brush> font-color: #1e293b;
}

export global ProductContainerStyles {
    out property <brush> background: #ffffff;
    out property <brush> font-color: #64748b;
    out property <length> font-size: 13px;
    out property <int> font-weight: 400;

    out property <length> width: 200px;
    out property <length> height: 260px;

    out property <length> image-width: 160px;
    out property <length> image-height: 110px;
    out property <brush> border-color: #e2e8f0;

    out property <length> title-font-size: 14px;
    out property <int> title-font-weight: 600;
    out property <brush> title-color: #1e293b;

    out property <length> category-font-size: 11px;
    out property <int> category-font-weight: 500;
    out property <brush> category-font-color: #ffffff;
    out property <brush> category-background: #3b82f6;
    out property <length> category-border-radius: 4px;
    out property <length> category-width: 110px;
    out property <length> category-height: 22px;
}

export global CatalogStyle {
    out property <length> spacing: 12px;
    out property <length> x: 8px;
}

export global LoginStyle {
    out property <brush> background: #1a2f4a;
    out property <brush> font-color: #ffffff;
    out property <length> font-size: 16px;
    out property <int> font-weight: 600;
}

export global TableStyle {
    out property <length> col-dimensions: 130px;
    out property <length> col-packaging: 110px;
    out property <length> col-standard: 165px;
    out property <length> col-description: 150px;
    out property <length> col-price-unit: 130px;
    out property <length> col-price-total: 120px;
    out property <length> col-action: 160px;
    out property <length> row-height: 60px;
    out property <brush> header-background: #f8fafc;
    out property <brush> row-even-background: #ffffff;
    out property <brush> row-odd-background: #f8fafc;
    out property <brush> border-color: #e2e8f0;
    out property <length> font-size: 13px;
    out property <int> font-weight: 400;
    out property <brush> font-color: #1e293b;
}

export global DetailStyle {
    out property <length> image-width: 280px;
    out property <length> image-height: 200px;
    out property <brush> title-color: #1e293b;
    out property <length> title-font-size: 20px;
    out property <int> title-font-weight: 700;
    out property <brush> category-color: #3b82f6;
    out property <length> category-font-size: 13px;
    out property <brush> font-color: #1e293b;
}
